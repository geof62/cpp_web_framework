#ifndef         FRAMEWORK_ROUTER_ROUTING_CALLS
# define        FRAMEWORK_ROUTER_ROUTING_CALLS

# include       "Router/RouteCollectionInterface.hpp"

namespace Router
{
    const Framework::Router::RouteCollectionInterface     &getRouting(void);
}

#endif